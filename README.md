# Real-time-Streaming-Pipeline
Built a real-time data pipeline using Kafka and Spark Structured Streaming to ingest, process, and analyze user events with fault-tolerant, exactly-once semantics. Implemented a Medallion architecture (Bronze, Silver, Gold) with Delta Lake, handling late-arriving data, deduplication, and generating business KPIs in near real-time.
